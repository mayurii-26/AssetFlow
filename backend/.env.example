PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Database
DATABASE_URL="mysql://assetflow_user:AssetFlow@2024@localhost:3306/assetflow_db"

# JWT
JWT_SECRET=assetflow_super_secret_change_in_production_2024
JWT_EXPIRES_IN=7d

# SMTP — use Mailhog locally: docker run -d -p 1025:1025 -p 8025:8025 mailhog/mailhog
# For Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, use App Password
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=AssetFlow <noreply@assetflow.com>
